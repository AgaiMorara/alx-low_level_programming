#include <stdio.h>

/**
 *main - program that prints numbers base 10
 *program implements the basic for loop
 *Return: int 0 if successful
 */

int main(void)
{
	int N;

	for (N = 0; N < 10; N++)
	{
		printf("%i\n",N);
	}
	return (0);
}
