#include "main.h"

/**
 *swap_int- swaps values of two integers variables
 *@a:the first variable
 *@b:the second variable
 *
 */

void swap_int(int *a, int *b)
{
	temp = *b;
	*b = *a;

	*a = temp;
}
