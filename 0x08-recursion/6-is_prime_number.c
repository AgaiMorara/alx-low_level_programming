#include "main.h"

/**
 *is_prime_number - checks wether n is prime
 *@n:  input for the prime function
 *
 *Return: 1 if is a prime number, 0 otherwise
 */
int is_prime_number(int n)
{
	if (n < 2)
		return (0);
	else if (n % x == 0)
		return (1);
	return (n ** (1 / 2));
}
