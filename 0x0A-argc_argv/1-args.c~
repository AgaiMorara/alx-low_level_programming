#include <stdlib.h>
#include <stdio.h>

/**
 * main -entry point
 *@argc: size of arguments
 *@argv: a pointer to a vector of pointers to the string arguments
 *Return: 0 - success
 */
int main(int argc __attribute__((unused)), char *argv[] __attribute__((unused)))
{
	printf("%d\n", argc - 1);
	return (0);
}
