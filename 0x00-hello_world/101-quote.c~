#include <stdio.h>

/**
 *main - program uses printf to print a statement
 *escapes at the beginning
 *Return: 0 program finished successfully
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
