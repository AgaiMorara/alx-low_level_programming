#include "main.h"
/**
 *_abs - computes the absolute value
 *@n: integer whose sign should be printed
 *Return: abs
 */
int _abs(int n)
{
	int i;

	if (n > 0)
	{
		i  = n;
	}
	else
	{
		i = n * -1
	}
	return (i);
}
