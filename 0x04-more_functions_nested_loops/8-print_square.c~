#include "main.h"

/**
 * print_square - draws a square using #
 *@size: the length of a side of the square
 *
 */

void print_square(int size)
{
	int x, y;

	if (size < 1)
		_putchar(10);
	for (x = 0; x < size; x++)
	{
		for (y = 0; y < size ; y++)
			_putchar(35);
		_putchar(10)
	}

}
